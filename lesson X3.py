# encapsulation. cordoning off your code into a specific namespace.
    # private: member (method/attribute) is only availible within a specific class namespace
    # protected: member is only availible with a specific class and the subclasses of said class
    # public: the default usage of members, can be used anywhere in the script
# 